📁 Step 5: Create the pages app
powershell
python manage.py startapp pages
📁 Step 6: Register the app
Open myproject/settings.py and add 'pages', to INSTALLED_APPS:

python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'pages',          # 👈 Add this line
]
📁 Step 7: Create the views
Open pages/views.py and replace with:

python
from django.http import HttpResponse

def home(request):
    return HttpResponse("Home Page")

def about(request):
    return HttpResponse("About Page")

def contact(request):
    return HttpResponse("Contact Page")
📁 Step 8: Create app URLs
Create a new file pages/urls.py and add:

python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.home, name='home'),
    path('about/', views.about, name='about'),
    path('contact/', views.contact, name='contact'),
]